# GUI Module

This package provides a Tkinter-based single-window multi-tab GUI for SPMiner workflows.

## Entry Point

```bash
python -m src.gui.main_window
```

## Notes

- Data browsing is read-only.
- Runtime output streams into the log panel.
- Long-running tasks run in a background worker thread.
